# 沛警智枢接口对齐记录

状态取值：`现有可用`、`现有待扩展`、`后端新增`、`等待正式契约`、`Mock可联调`、`正式联调完成`。

| 编号 | 页面动作 | 接口 | 当前状态 | 本次实现 | 后端对齐事项 |
| --- | --- | --- | --- | --- | --- |
| AUTH-01 | 账号登录 | `POST /auth/login` | 现有待扩展 | 增加成功/失败日志和最近登录 | 确认账号初始化来源 |
| AUTH-02 | 当前用户 | `GET /me` | 现有待扩展 | 增加姓名、警号、部门、职务、系统角色 | 对齐组织同步策略 |
| CHAT-01 | 会话与消息 | `/sessions/*` | 现有可用 | 消息使用 `X-Analysis-Mode` 传递研判模式并返回持久化 `run_id` | Runtime 完成状态回写 |
| CHAT-02 | 统一能力选择 | `GET /capabilities` | 后端新增 | 已聚合个人 Skill、授权插件和官方能力 | 确认能力分类字典 |
| CHAT-03 | Skill 草稿 | `/skill-drafts/*` | 后端新增 | 已提供需求/对话生成、编辑、测试、保存 | 后续替换为正式模型提炼 |
| RUN-01 | 运行详情 | `GET /sessions/{sid}/runs/{runId}` | 后端新增 | 已建立运行、事件和审计关联 | Runtime 回调更新步骤状态 |
| RUN-02 | 执行轨迹 | `GET .../events` | 后端新增 | 已返回可回放结构化步骤 | 增加 SSE 增量推送 |
| RUN-03 | 证据链 | `GET .../evidence` | Mock可联调 | 已提供统一结论、标签和链条结构 | 正式数据接口回填证据记录 |
| RUN-04 | 重新研判 | `POST .../rerun` | 后端新增 | 已生成关联的新运行 | 对接 Runtime 实际重跑 |
| RUN-05 | 报告导出 | `GET .../report` | 后端新增 | 已提供 Markdown 报告 | 确认最终 Word/PDF 格式 |
| MODEL-01 | 模型列表与维护 | `/admin/models` | 现有待扩展 | 增加 Provider、上下文、接入方式、工具调用和测试状态 | 确认本地部署执行器 |
| USER-01 | 用户维护 | `/admin/users` | 现有待扩展 | 增加业务身份字段和筛选 | 确认用户初始清单 |
| DEPT-01 | 部门树 | `/admin/departments/*` | 后端新增 | 已提供查询、新增、编辑和受限删除 | 确认组织编码及同步方式 |
| AUDIT-01 | 调用审计 | `/admin/invocations` | 后端新增 | 已提供列表、详情、筛选基础和 CSV 导出 | Runtime 完成时回写耗时和记录数 |
| CAP-01 | 官方能力配置 | `/admin/capabilities` | 后端新增 | 已提供配置、启停、依赖和测试 | 确认正式能力目录 |
| BIZ-01 | 人员基本信息 | 后端工具适配层 | 等待正式契约 | 采用统一响应结构 | 校验现有文档字段、鉴权和错误码 |
| BIZ-02 | 家庭关系 | 后端工具适配层 | 等待正式契约 | 采用统一响应结构 | 校验关系类型字典 |
| BIZ-03 | 人员轨迹 | 后端工具适配层 | 等待正式契约 | 证据链已可接 Mock | 校验位置、时间和分页字段 |
| BIZ-04 | 涉案/涉警人员 | 后端工具适配层 | 等待正式契约 | 采用统一响应结构 | 确认数据权限透传参数 |
| BIZ-05 | 场所/同行/车辆 | 后端工具适配层 | Mock可联调 | 前端按统一证据结构展示 | 待后端提供正式契约 |

## 统一业务响应

```json
{
  "data": [],
  "record_count": 0,
  "source": "mock-or-service-id",
  "queried_at": 0,
  "trace_id": "trace-id",
  "error": null
}
```

## 联调记录要求

每次接口对齐必须补充：接口版本、负责人、请求示例、脱敏响应示例、鉴权方式、超时、分页、错误码、Mock 与正式环境地址、首次联调时间和遗留问题。任何凭证只登记存储位置，不登记明文。
